services:
  node-card-web:
    build:
      context: .
    image: node-card-web:latest
    container_name: node-card-web
    environment:
      API_PROXY_TARGET: ${API_PROXY_TARGET:-https://api.node-card.com}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
